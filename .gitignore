node_modules/
build/
dist/
out/
artifacts/
release/*.dmg
release/*.zip
.zmux/
.vsmux/
.DS_Store
.vscode/
.fresh/
AGENTS.md
.agents/skills/zmux-release-to-brew/SKILL.md
storybook-static/
native/macos/zmuxHost/Web/
native/macos/zmuxHost/Vendor/
native/macos/zmuxHost/build/
native/macos/zmuxHost/zmuxHost.xcodeproj/
native/macos/zmuxHost/zmux.xcodeproj/

# CDXC:PublicRelease 2026-04-27-05:36: Keep local agent instructions,
# generated Storybook output, native build products, and runtime logs out of
# the public repository while preserving each developer's local copies.
# CDXC:Distribution 2026-04-28-04:19: Release DMG/ZIP artifacts are uploaded to
# GitHub Releases and referenced by Homebrew casks, so git should track source
# and metadata only instead of committing binary distribution outputs.
# CDXC:RepoSafety 2026-05-02-00:11: The zmux release-to-brew skill is local
# agent workflow documentation and must remain available on this machine without
# appearing in the public GitHub repository tree.
# CDXC:RepoSafety 2026-05-10-10:21: Fresh workspace state records local window
# layout for this developer machine and should not be published with zmux source
# or release metadata.
.idea
react-doctor.config.json
.ignore
.ignored/

# CDXC:ReleaseDocs 2026-05-12-13:09: Local planning notes that should not be
# published with product docs live under `.ignored/` instead of needing
# per-file ignore rules.
