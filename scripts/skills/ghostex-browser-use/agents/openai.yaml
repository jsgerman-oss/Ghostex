interface:
  display_name: "ghostex-browser-use"
  short_description: "Use Ghostex CEF panes from agents"
  default_prompt: "Use $ghostex-browser-use to configure `ghostex browser mcp` access to Ghostex embedded browser panes."

policy:
  allow_implicit_invocation: true
