interface:
  display_name: "ghostex-generate-title"
  short_description: "Generate and stage a Ghostex session title"
  default_prompt: "Use $ghostex-generate-title to create a title under 47 characters and stage `/rename <title>` in the current Ghostex session."

policy:
  allow_implicit_invocation: true
