# CDXC:NativeGhosttySource 2026-05-17-00:13: Ghostty source is committed under /ghostty so the parent repo language breakdown includes the embedded terminal dependency's Zig source while build scripts still treat it as an upstream source dependency.
ghostty/** -linguist-vendored
ghostty/** -linguist-generated
ghostty/** -linguist-documentation

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads
